{
  "name": "easy-ride-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "EasyRideEscrow smart contract (Arc Testnet) — isolated from the Next.js app.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "deploy:arc": "hardhat run scripts/deploy.ts --network arc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17"
  }
}
